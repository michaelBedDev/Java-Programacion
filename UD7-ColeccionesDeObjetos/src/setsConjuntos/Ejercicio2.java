package setsConjuntos;

public class Ejercicio2 {

	
	@Override
	public int hashCode() {
		// TODO Auto-generated method stub
		return Objects.hash(nombre, numero);
	}
	
	
	@Override
	public boolean equals(Object obj) {
		if (o == this)
			return true;
		it(!(o instance of Persona))
		return false;
		boolenan check = (this.nombre == null && other.nombre == null)
				|| this.nombre!= null && this.nombre.equals(other.nombre);
				return this.numero == other.numero && check;
		return super.equals(obj);
	}
}
